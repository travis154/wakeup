-if(post.me == false)
	-if(!post.following || post.following == false)
		a#follow-user.margin-zero.span1.btn.btn-small.follow-btn(data-id=post.user._id.toString()) Follow
	-else
		a#follow-user.margin-zero.span1.btn.btn-small.unfollow-btn(data-id=post.user._id.toString()) Unfollow

